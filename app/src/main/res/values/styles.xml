<resources>

    <style name="LodaButton" parent="android:Widget.DeviceDefault.Button">
        <item name="android:background">@drawable/button</item>-
        <item name="android:textColor">@color/light2</item>
    </style>

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">-->
    <!--<style name="AppTheme" parent="android:Theme.Holo.Light">-->
    <!--<style name="AppTheme" parent="android:Theme.DeviceDefault">-->
    <style name="AppTheme">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@color/light1</item>
        <item name="android:textSize">15dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:buttonStyle">@style/LodaButton</item>
        <item name="android:divider">@drawable/divider</item>
        <item name="android:showDividers">middle</item>
    </style>

</resources>
